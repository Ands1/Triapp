<%= form_with(model: user_topic_answer) do |form| %>
  <% if user_topic_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_topic_answer.errors.count, "error") %> prohibited this user_topic_answer from being saved:</h2>

      <ul>
        <% user_topic_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
